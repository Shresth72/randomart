# Entry Node
E | vec3(C, C, C)
  ;

# Terminal Nodes
A | random
  | x
  | y
  | t
  | sqrt(add(mult(x, x), mult(y, y))) # distance
  ;

# Binary Operations
C ||  A               # 1/4
  ||| add(C, C)       # 3/8
  ||| mult(C, C)      # 3/8
  ;
